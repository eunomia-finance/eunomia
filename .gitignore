# Rust's output directory
target

# Local settings
.soroban
.stellar

# Node / build / OS
node_modules
dist
.DS_Store
*.log
.env
.env.local

# ZK build artifacts (circuits/contracts)
build/
*.zkey
*.ptau
*.wtns
circuits/build/
circuits/circuits/test/
# circomkit auto-generated entrypoint
circuits/circuits/main/

# Tooling state (superstack / cso reports)
.superstack/

# parametric prover scratch input (default.json is the committed fixture source)
circuits/inputs/compliance/live.json

# design audit (concept HTML + screenshots — not shipped)
web/.design-audit/



.vercel

# Assembled design canvases (2 MB editor payload) - re-seed from the .dc.html artboards
web/docs/design/canvas/eunomia-*.html
